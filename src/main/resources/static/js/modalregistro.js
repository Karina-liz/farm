document.addEventListener('DOMContentLoaded', () => {
    const modal = document.getElementById('modal');
    if (modal) {
        // Puedes agregar lógica para cerrar el modal si es necesario
    }
});
